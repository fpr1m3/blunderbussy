# Change prefix to Ctrl-a (more ergonomic)
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Enable mouse support
set -g mouse on

# Increase history for reviewing outputs
set -g history-limit 50000

# Start numbering at 1 for easier access
set -g base-index 1
setw -g pane-base-index 1
