# MCP Servers Dependencies
# Agent Opulence - Stream C: Custom MCP Servers

# YAML parsing for artifact and manifest handling
PyYAML>=6.0.1

# Optional: For enhanced JSON schema validation
# jsonschema>=4.20.0

# No external MCP SDK required - using native JSON-RPC over stdio
# The servers implement the MCP protocol directly for maximum control
