services:
  eval-target:
    image: docker.io/vulnerables/cve-2014-6271
    container_name: eval-bash-shellshock
    ports:
      - "8083:80"
    networks:
      - eval-net

networks:
  eval-net:
    driver: bridge
