services:
  eval-target:
    image: docker.io/vulhub/libssh:0.8.1
    container_name: eval-libssh-auth-bypass
    ports:
      - "2222:22"
    networks:
      - eval-net

networks:
  eval-net:
    driver: bridge
