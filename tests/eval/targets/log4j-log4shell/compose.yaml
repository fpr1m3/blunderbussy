services:
  eval-target:
    image: docker.io/vulhub/solr:8.11.0
    container_name: eval-log4j-log4shell
    ports:
      - "8983:8983"
    networks:
      - eval-net

networks:
  eval-net:
    driver: bridge
