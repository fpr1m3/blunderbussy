quick_wins:
- priority: 1
  reason: Known CVE with public exploits targeting Samba
  vector: CVE-2017-7494 exploit
services:
- name: smb
  port: 445
  product: Samba
  protocol: tcp
  version: 4.6.3
  vulnerabilities:
  - cve: CVE-2017-7494
    description: Samba since version 3.5.0 and before 4.6.4, 4.5.10 and 4.4.14 is
      vulnerable to remote code execution vulnerability, allowing a malicious client
      to upload a shared library to a writable share, and then cause the server to
      load and execute it.
    severity: critical
    title: Samba since version 3.5.0 and before 4.6.4, 4.5.10 and 4.4.14 is vulnerable
      to r
target:
  hostname: eval-sambacry
  ip: EVAL_TARGET_IP
  platform: linux
