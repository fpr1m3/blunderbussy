services:
  eval-target:
    image: docker.io/vulhub/spring-webmvc:5.3.17
    container_name: eval-spring4shell
    ports:
      - "8086:8080"
    networks:
      - eval-net

networks:
  eval-net:
    driver: bridge
